# Docs

Navigation hub for `docs/`. (Project overview lives in the repo-root [`README.md`](../README.md).)

| File | What it is |
| --- | --- |
| [`master-spec.md`](master-spec.md) | The source of truth — product vision, scope, architecture, the milestone outline, and a per-milestone "Adjustments during the build" record. Start here for the big picture. (Bilingual: English headings, mixed prose — kept as originally written.) |
| [`milestone-1.md`](milestone-1.md) | The M1 plan (`v0.1.0-m1`) — universal client foundation: 24 atomic commits + acceptance criteria, the repo-layout deltas (Δ1–Δ8), and an "Adjustments during the M1 build" section. Frozen as a historical record. |
| [`milestone-1.5.md`](milestone-1.5.md) | The M1.5 plan (`v0.1.5-m1.5`) — OAuth 2.1 + PKCE as a third auth method: commits C25–C32 + a "Build adjustments" section. |
| [`milestone-2.md`](milestone-2.md) | The M2 plan (`v0.2.0-m2`) — plugin architecture + the read-only Niagara explorer: commits C33–C47 in four phases, kickoff decisions, check-in points, and an "Adjustments during the M2 build" section (incl. the found-while-testing fixes). |
| [`milestone-3.md`](milestone-3.md) | The M3 plan (`v0.3.0-m3`) — write & safety: the recon decisions (write workflow, annotation overrides, BearerResolver, property-sheet edit, history-viewer scope, e2e fixture), commits C48–C60 in four phases with check-in points, and an "Adjustments during the M3 build" section. |
| [`milestone-4.md`](milestone-4.md) | The M4 plan (`v0.4.0-m4`) — observability: the recon decisions (charting lib, live-monitor state model, history-viewer scope, watch persistence, perf-timeline data source, tool-usage stats, chart-primitives reuse for M5), commits C61–C68 in four phases with check-in points, and an "Adjustments during the M4 build" section. |
| [`milestone-5.md`](milestone-5.md) | The M5 plan (`v0.5.0-m5`) — AI co-pilot: the recon decisions (LLM provider abstraction, ReAct loop, per-connection multi-conversation scope, workspace-global LLM key, the safety-boundary at `ConnectionManager.callTool`, streaming UX, plugin contributions, `<chart>`-inline rendering, mock LLM provider for e2e), commits C69–C80 in four phases with check-in points, and an "Adjustments during the M5 build" section. Source-of-truth vision: [`handover.md`](../handover.md) Part 2. |
| [`m1-followups.md`](m1-followups.md) | Things deferred from M1 / M1.5 — UI/renderer, quality/tooling, later-milestone architecture, the M1.5/OAuth follow-ups, and the **niagaramcp-side coordination list** (changes needed on niagaramcp, tracked here because they affect Studio). |
| [`m2-followups.md`](m2-followups.md) | Things deferred during M2 — tree virtualisation, the property-sheet links/extensions panel, a host `ctx.openView` hook + per-node context menu, slimming the BqlView chunk + a dark editor theme + a Lezer BQL grammar, per-connection explorer state, a deeper schema merge. |
| [`m3-followups.md`](m3-followups.md) | Things deferred during M3 — plugin-api seams (`onBeforeDisconnect` / `ctx.openView` / `ctx.updateAuthSecret` / `ctx.reconnect`); UX polish (issue-the-inverse / `Delete` shortcut / orphaned-queue notification / visual wire-mode); enum + complex-slot editors; the Zustand singleton gotcha; replicated niagaramcp coordination items relevant to M3+M4. |
| [`m4-followups.md`](m4-followups.md) | Things deferred during M4 — CodeMirror manualChunks split (when a second CM6 consumer appears); `useExplorerStore.known` as a cross-view shared primitive (semantics + eviction policy); `ctx.workspace.watches` plugin-api seam; true LTTB downsampler + uPlot fallback; batched-read for the watch list; per-watch threshold notifications; drag-and-drop e2e coverage; per-series history ranges; semantic error-code coloring; dedicated perf-repo for longer retention. |
| [`screenshots/`](screenshots/) | Check-in / milestone-boundary snapshots of the running app. The M2 + M3 + M4 shots are captured by the Playwright e2e specs themselves (gated on `MCPSTUDIO_E2E_SCREENSHOTS=1` so a normal `pnpm test:e2e` run doesn't rewrite them); the M1-era shots come from the single-shot `MCPSTUDIO_CAPTURE_PATH` main-process hook. `screenshots/README.md` indexes them. |
